import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  component: {
    borderRadius: 1,
    overflow: 'hidden',
  },
  imageStyle: {
    width: '100%',
    height: '100%',
  },
  defaultImageStyle: {
    width: 100,
    height: 100,
  },
});
