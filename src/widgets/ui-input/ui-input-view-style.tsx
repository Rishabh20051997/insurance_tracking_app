import {StyleSheet} from 'react-native';

import colors from '@resources/colors';

export const inputStyle = StyleSheet.create({
  component: {
    backgroundColor: colors.gray_scale.dove_gray,
    padding: 5,
    borderRadius: 5,
    marginTop: 20,
  },
});
