import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textInputStyle: {marginVertical: 4, width: '100%'},
  textInputBackgroundStyle: {backgroundColor: '#ffffff'},
});
